# M07-S14 — Slide 14 visual source

**`MODULE 7 VISUAL SOURCE — ACCEPTED under T7-K`**

**`NATIVE ORIGINAL DIAGRAM — SOURCE DEFINITION`**

**`NOT TRIVIRON GOVERNANCE`**

**Teaching material. Not governance.** This file implements one accepted visual
specification. It explains; it decides nothing, approves nothing and establishes
nothing about Triviron.

**This is a native construction specification — not a picture, not a rendered
asset, not a PowerPoint slide, not a screenshot, not a governance record, not
Triviron evidence and not a Triviron answer.**

> **This is a teaching workflow only. No connector crosses the authority gate.
> No connector enters the final controlled-destination box.**
>
> **Geometry is the accepted `T7-J-R2` reconciliation, used exactly.**

---

## Header

| # | Field | Value |
|---:|---|---|
| 1 | **Visual-source identifier** | `M07-S14` |
| 2 | **Controlling visual specification** | **`M7V-14`** — [`visual-demonstration-plan.md`](../../module-07-harrismith-to-triviron-bep-translation/visual-demonstration-plan.md) §4, **`ACCEPTED after T7-J-R + T7-J-R2`** |
| 3 | **Slide number** | 14 |
| 4 | **Slide title** | From decision backlog to BEP workshop · **1.5 min** — allocated, not measured |
| 5 | **Visual title** | A teaching workflow, an authority gate that stops it, and a roadmap close that schedules nothing |
| 6 | **Increment** | **`T7-K`** — visual-source production |
| 7 | **Source class** | **`NATIVE ORIGINAL DIAGRAM`** |
| 8 | **Production status** | Source **`SOURCE COMPLETE — ACCEPTED`** · rendered **`NONE`** · assembly **`NOT STARTED`** · PowerPoint **`NONE`** |
| 9 | **Teaching purpose** | Close Module 7 by showing what future governed work must accomplish before a project-specific Triviron BEP could responsibly be populated, and bridge to Module 8 |
| 10 | **Five-second takeaway** | *The answers have to be earned, and nothing here says when.* |
| 11 | **Principal sources** | [`translation-framework.md`](../../module-07-harrismith-to-triviron-bep-translation/translation-framework.md) §1–§2; [`translation-decision-register.md`](../../module-07-harrismith-to-triviron-bep-translation/translation-decision-register.md) §9–§10; [`translation-decision-register-schema.md`](../../module-07-harrismith-to-triviron-bep-translation/translation-decision-register-schema.md) §5, §7; [`../../roadmap.md`](../../roadmap.md) Module 8 row; accepted outline §16 |
| 12 | **Source authority classification** | Framework, register and schema — **accepted Module 7 architecture / accepted register**. The roadmap — **teaching plan, `TEACHING INTERPRETATION — NOT GOVERNANCE`**. **The central statement is `TEACHING SYNTHESIS`** (§11) |
| 13 | **Register rows engaged** | **Register-wide** — the register is the decision backlog the future workflow would consume; **its zeros are the starting position handed to Module 8, unchanged.** No individual row appears |
| 14 | **Translation classifications shown** | **None as a value.** `candidate decision` and `recorded status` in the method one-liner are **step names**, not classification or status values |
| 15 | **Status vocabulary shown** | **None as a value.** `recorded status` is a step name only, and is labelled as such |
| 16 | **Evidence position shown** | **`the backlog handed forward is unchanged — 21 rows, evidence NONE IDENTIFIED × 21, candidate decisions 0`** |
| 17 | **Fixed visual form** | A declared teaching-workflow band in two registers separated by a full-width authority gate strip, a method one-liner, a three-segment roadmap close, and a boundary strip |
| 18 | **Known limitation** | Stage labels are compressed to three or four words; the full workflow wording is in the accepted outline §16 field 6 |
| 19 | **Overclaim risk** | **The closing slide is where a teaching workflow most easily becomes a project plan.** Three hazards: seven stages read as a schedule; `appropriate authority engaged` reads as *someone has been identified*; `Next: Module 8` reads as *the workshop is next* |
| 20 | **External imagery · live evidence · Mermaid** | `NONE REQUIRED` · `NONE REQUIRED` · **`Mermaid PROHIBITED`** |
| 21 | **Rendered-asset status** | **`NONE`.** No rendering attempted |

---

## 1. Purpose and five-second takeaway

**Purpose.** Close Module 7 by showing what future governed work must accomplish
before a project-specific Triviron BEP could responsibly be populated, and bridge
to Module 8.

**Five-second takeaway.** *The answers have to be earned, and nothing here says
when.*

**Why this form.** The accepted workflow has seven stages and **one of them is an
authority engagement that may produce nothing**. Drawing seven boxes in a run
would make the workflow look like a process that completes. Splitting it at the
authority gate makes the conditionality structural. **The header band is the
second control:** without an explicit `TEACHING WORKFLOW` declaration, a
seven-stage diagram on a closing slide reads as the project's plan (`M7-S11`,
`M7V-S30`).

**Simplification rule.** Stage labels may compress to three or four words. **No
stage may be deleted, reordered, merged or moved across the gate strip.** **The
header declaration, the gate strip, the complete method one-liner and the
boundary strip may never be shortened or reduced below 18 pt.** **The method
one-liner appears in full, all nine objects, or not at all** — a partial chain
would omit the STOP. **The header declaration's second semantic statement may
wrap onto a second physical line, and the semicolon is the only permitted break
point — a layout break only, altering no wording.** If space is short, compress
stage labels and roadmap segment text.

**If the native build cannot reproduce the header wording at ≥ 18 pt within the
current header band, the producer STOPS and refers back — type is never silently
shrunk, condensed or letter-spaced to force a fit** (rule **G8**). **`T7-L-R6`:
the current header band is `y = 84–184`, exactly `100 pt`, on frame A. The
superseded one-frame band was `y = 84–148`.**

## 2. Source and authority basis

**Controlling specification.** `M7V-14`, accepted as `T7-J — ACCEPTED after
T7-J-R + T7-J-R2`. **The evidence-boundary wording is the `T7-J-R` correction;
the geometry is the `T7-J-R2` reconciliation.**

**Controlling teaching content.** Accepted
[`presentation-outline.md`](../../module-07-harrismith-to-triviron-bep-translation/presentation-outline.md)
§16 — Slide 14, `ACCEPTED after T7-G-R`.

**Register / source evidence.** Framework §1–§2 (sequence and STOP rule);
register §9–§10 (the backlog handed forward); schema §5, §7 (authority and
candidate prerequisites). Class F is **`NONE IDENTIFIED`**.

**Authority chain.**

```text
controlled Harrismith sources
  ↓
accepted Module 7 teaching content
  ↓
accepted Module 7 visual-demonstration plan   ← M7V-14, ACCEPTED after
                                                 T7-J-R + T7-J-R2
  ↓
Module 7 visual-source file                   ← this file, T7-K, ACCEPTED
  ↓
presentation assembly package (T7-L)          ← NOT STARTED
  ↓
future external PowerPoint (T7-M)             ← NOT STARTED
```

**The order runs downward and never upward. This file does not reinterpret
`M7V-14`.**

## 3. Classification, status and evidence position

| | |
|---|---|
| **Translation classification** | **None shown as a value** |
| **Register status** | **None shown as a value.** `recorded status` is a **step name**, labelled as such |
| **Evidence position** | `21 rows` · `evidence NONE IDENTIFIED × 21` · `candidate decisions 0` — **the backlog handed forward, unchanged** |

**Classification ≠ status.** Neither vocabulary appears as a value, so neither
may be introduced by a producer. **`candidate decision` and `recorded status` are
stage and step names only.**

## 4. Fixed layout and object hierarchy

| Rank | Object | Rule |
|---:|---|---|
| 1 | The header declaration | Full width — **the slide's governing label** |
| 2 | **The authority gate strip** | Full width, border ≥ 3 pt — a principal object |
| 3 | The two workflow registers | **Co-equal.** **Register 2 is not styled as unavailable or greyed** — it is a later part of a teaching workflow, not a blocked one |
| 4 | The method one-liner | Full width |
| 5 | The roadmap close | Three segments |
| 6 | The boundary strip | Full width |

**Register 1, left to right:** `decision backlog` · `evidence gathering` ·
`decision agenda` · `appropriate authority engaged`.
**Register 2, left to right:** `candidate decision, if authorised` ·
`decision recorded with status` ·
`BEP / matrix / schedule / appendix populated only when earned`.

## 5. Exact visible wording

**Header declaration — two semantic statements, verbatim**

- **`TEACHING WORKFLOW — NOT A CURRENT TRIVIRON WORKFLOW`**
- **`no actual Triviron workshop or schedule is established in this programme;
  this diagram does not assert or schedule one`**

**Authorised physical layout — three text lines, wording unchanged**

1. `TEACHING WORKFLOW — NOT A CURRENT TRIVIRON WORKFLOW`
2. `no actual Triviron workshop or schedule is established in this programme;`
3. `this diagram does not assert or schedule one`

**The semicolon is the only permitted break point. The break is a layout break
only, and no word, mark or qualifier may be altered, moved between statements or
dropped.**

**Register 1**

`decision backlog` · `evidence gathering` · `decision agenda` ·
`appropriate authority engaged`

**Gate strip, verbatim — both lines**

- **`AUTHORITY REQUIRED — no automatic progression`**
- **`evidence ≠ authority · no simulated Triviron decision function has been
  authorised, and no appropriate Triviron authority holder is currently
  identified or established in this programme`**

**Register 2**

`candidate decision, if authorised` · `decision recorded with status` ·
`BEP / matrix / schedule / appendix populated **only when earned**`

**Method one-liner — complete and verbatim, all nine objects**

- **`Harrismith reference pattern → translation question → Triviron evidence
  required → evidence available / missing → STOP → candidate decision →
  authority required → recorded status → controlled destination`**
- labelled **`analytical sequence — nothing flows along these arrows`**

**Roadmap close — three segments**

- **`Completed teaching foundation: Modules 1–6`**
- **`Module 7: translation method / decision backlog`**
- **`Next: Module 8 — BEP workshop facilitation`**

**Boundary strip — all three, together**

- **`the future workshop is not "copy Harrismith into Triviron" and not "fill in
  the blanks" — it is a controlled decision-making process in which the
  questions, required evidence, authority and status are made explicit`**
- **`the backlog handed forward is unchanged — 21 rows, evidence NONE IDENTIFIED
  × 21, candidate decisions 0`**
- **`TEACHING SYNTHESIS: a good BEP workshop does not start with answers. It
  starts with the decisions that must be earned.`**

## 6. Native object inventory

> **`T7-L-R6` — SUPERSEDED ONE-FRAME RECORD, RETAINED AS HISTORY.** This inventory
> describes the single physical slide that `T7-L-R3` proved infeasible. **It is not
> a current build instruction.** **The current production geometry is the two-frame `T7-L-R5` A/B geometry** —
see the **`T7-L-R4` / `T7-L-R5` GOVERNED SPLIT PAGINATION** section below.
Frame A: header `y = 84–184` (**100 pt**) · Register 1 `y = 192–268` · authority
gate `y = 276–376` · Register 2 `y = 384–480` · **all four connectors**.
Frame B: method chain `y = 84–216` · roadmap close `y = 224–296` · boundary strip
`y = 304–520` · **zero connectors**.
> **The physical object inventory to build is in
> [`../../module-07-harrismith-to-triviron-bep-translation/presentation/asset-manifest.md`](../../module-07-harrismith-to-triviron-bep-translation/presentation/asset-manifest.md)
> §7, rows 17 and 18.**

| Count | Object | Carries |
|---:|---|---|
| 1 | Full-width rectangle, `y = 84–148` | The header declaration — **one text box holding both semantic statements across three physical lines** |
| 4 | Rectangles with text boxes | Register 1, stages 1–4 |
| 3 | Straight arrow connectors | Within register 1 only |
| 1 | Heavy-bordered full-width rectangle | The authority gate strip, two text runs |
| 3 | Rectangles with text boxes | Register 2, stages 5–7 |
| 1 | Straight arrow connector | Stage 5 → stage 6 **only** |
| 1 | Full-width text box | The method one-liner — **arrows as typographic characters, not drawn connectors** |
| 3 | Rectangles | The roadmap close |
| 1 | Bordered full-width rectangle | The boundary strip, three text runs |

**Four connectors in total. No connector touches the gate strip, the final stage
box, the one-liner, the roadmap close or the boundary strip.**

## 7. Geometry — accepted `T7-J-R2` reconciliation · SUPERSEDED

> **`T7-L-R6` — SUPERSEDED ONE-FRAME GEOMETRY, RETAINED AS HISTORY.** The
> coordinate arithmetic below is verified and is **not withdrawn as a record**, but
> **it is withdrawn from production**: `T7-L-R3` proved this single-frame layout
> cannot carry the required copy at the accepted type floors, and its `64 pt`
> header band is arithmetically wrong for three 18 pt lines (which need `72.8 pt`).
> **Do not build these coordinates.** **The current production geometry is the two-frame `T7-L-R5` A/B geometry** —
see the **`T7-L-R4` / `T7-L-R5` GOVERNED SPLIT PAGINATION** section below.
Frame A: header `y = 84–184` (**100 pt**) · Register 1 `y = 192–268` · authority
gate `y = 276–376` · Register 2 `y = 384–480` · **all four connectors**.
Frame B: method chain `y = 84–216` · roadmap close `y = 224–296` · boundary strip
`y = 304–520` · **zero connectors**.

**Reference canvas `960 × 540 pt`, 16:9. Title zone `y = 0–72` reserved, with a
12 pt clear interval to the header. Content span exactly 864 pt, `x = 48–912`.
Lowest permitted object edge `y ≤ 520`.**

| Object | Geometry |
|---|---|
| **Header declaration** | `x = 48–912`, **`y = 84–148`** — **exactly 64 pt** |
| **Register 1**, four boxes | `x = 48–249`, `269–470`, `490–691`, `711–912`; **`y = 156–224`** — **each exactly 201 × 68 pt, three gaps of exactly 20 pt** |
| **Authority gate strip** | `x = 48–912`, **`y = 232–272`** — **exactly 40 pt**, border ≥ 3 pt |
| **Register 2**, three boxes | `x = 48–324`, `342–618`, `636–912`; **`y = 280–348`** — **each exactly 276 × 68 pt, two gaps of exactly 18 pt** |
| **Method one-liner** | `x = 48–912`, **`y = 356–412`** — **exactly 56 pt** |
| **Roadmap close** | `x = 48–324`, `342–618`, `636–912`; **`y = 420–464`** — **each exactly 276 pt, two gaps of exactly 18 pt**, 44 pt tall |
| **Boundary strip** | `x = 48–912`, **`y = 472–520`** — **exactly 48 pt** |

**All six inter-band gaps are exactly 8 pt.**

## 8. Geometry arithmetic — independent verification · SUPERSEDED

> **`T7-L-R6` — SUPERSEDED ONE-FRAME ARITHMETIC, RETAINED AS HISTORY.** This block
> verifies the §7 single-frame coordinates. **Its coordinate arithmetic is sound
> and is not withdrawn as a record, but it is withdrawn from production**, and its
> closing `header nominal fit: 3 × 18 pt = 54 pt inside a 64 pt region` line is
> **arithmetically wrong** — three 18 pt lines occupy `3 × 21.6 = 64.8 pt` of line
> height plus `8 pt` padding = **`72.8 pt`**. **Do not build from this block.**
> **The current per-frame arithmetic is in the `T7-L-R4` / `T7-L-R5` GOVERNED
> SPLIT PAGINATION section below**, where the header band is **`100 pt`** with a
> **`+27.2 pt`** margin.

Recalculated in `T7-K` against the accepted `T7-J-R2` geometry, not copied from
it.

```text
register 1:  4 × 201 + 3 × 20  =  804 + 60  =  864
content span:      912 − 48    =  864                                ✓
sequence:  48+201=249 ; 249+20=269 ; 269+201=470 ; 470+20=490 ;
           490+201=691 ; 691+20=711 ; 711+201=912                    ✓

register 2 and roadmap close:  3 × 276 + 2 × 18  =  828 + 36  =  864 ✓
sequence:  48+276=324 ; 324+18=342 ; 342+276=618 ; 618+18=636 ;
           636+276=912                                                ✓

vertical (seven bands and six 8 pt gaps):
  64 + 8 + 68 + 8 + 40 + 8 + 68 + 8 + 56 + 8 + 44 + 8 + 48  =  436
  520 − 84                                                   =  436
  therefore  436 = 436                                                ✓

band sequence, disjoint and every gap exactly 8 pt:
  header    84–148   | gap 148–156
  register1 156–224  | gap 224–232
  gate      232–272  | gap 272–280
  register2 280–348  | gap 348–356
  method    356–412  | gap 412–420
  roadmap   420–464  | gap 464–472
  boundary  472–520                                                   ✓ (G5)

title zone:  reserved y = 0–72 ; clear interval  84 − 72  =  12 pt    ✓
equality:  register 1 boxes all exactly 201 × 68 ; register 2 boxes
           and roadmap segments all exactly 276 ; both registers 68 pt ✓ (G3)
bounds:    x within 48–912 ; lowest edge exactly 520                  ✓ (G6)
ownership: the `only when earned` condition sits INSIDE its own box,
           and the gate strip sits BETWEEN the two registers it
           separates                                                   ✓ (G7)

header nominal fit:  3 × 18 pt  =  54 pt  inside a 64 pt region,
                     leaving 10 pt total nominal vertical allowance
                     beyond the three type lines.
  This is a production specification, not proof of final rendered
  typography — the producer must confirm actual fit WITHOUT reducing
  type, and STOP and refer back if it cannot be achieved.            (G8)
```

**`GEOMETRY ARITHMETIC — VERIFIED AGAINST ACCEPTED SPECIFICATION`**

### `T7-L-R3` — production-geometry correction status

**The coordinate arithmetic above is verified and remains verified. It was never
a production-fit claim, and it is not one now.**

| Claim | Status |
|---|---|
| **Coordinate arithmetic verified** | **VERIFIED** — spans, equalities, gaps and disjointness reconcile. Unchanged by `T7-L-R3` |
| **Production fit verified** | **NOT VERIFIED — `UNRESOLVED`.** The required visible copy cannot be set at `≥ 14 pt` / `≥ 18 pt` inside this geometry |

**Source status.** `M07-S14` remains **`ACCEPTED under T7-K`** as a source
definition. **Its production geometry is under governed `T7-L-R3` correction on
fabrication evidence** — the external `T7-M` build reached `7.8 pt` minimum type
with 146 runs below `14 pt`. **The original feasibility claim is not current.**

**Defect of record.** Slide 14 — declaration · Register 1 · authority gate · Register 2 · supporting regions. The seven regions need `607.2 pt` stacked, or `557.6 pt` with the authorised horizontal packing of the method chain beside the roadmap close; the budget is `436 pt`. Horizontal packing **saved `49.6 pt`** and is retained in the recommendation.

**Deficit at the best arrangement tested: `121.6 pt`.**

**Why repacking does not close it.** For a fixed string a narrower column produces more lines, so full-width stacking already minimises every long region. The header, the gate, the nine-object chain and the boundary strip are all long and all already minimal. **The accepted `3 × 18 pt = 54 pt inside 64 pt` header claim is superseded** — three 18 pt lines need `64.8 pt` of line height plus `8 pt` padding = `72.8 pt`, so the band must be at least `82 pt` to carry the declaration with the required margin. The declaration wording, its three physical lines and the semicolon break point are unchanged.

**`T7-L-R3` changed no coordinate in §7**, because no arrangement of this accepted
visual concept passes the type floors. The full arithmetic, every lever tested and
the options that would resolve it — each needing a governance decision — are in
[`../../module-07-harrismith-to-triviron-bep-translation/presentation/deck-specification.md`](../../module-07-harrismith-to-triviron-bep-translation/presentation/deck-specification.md)
§12.

**`T7-L-R3` was ACCEPTED, and that one-frame position is now SUPERSEDED for
production — see the `T7-L-R4` section below.**
**`T7-M-R1` is `NOT STARTED` and is not authorised.** **A produced PowerPoint
never governs this source.**

---

## `T7-L-R4` — GOVERNED SPLIT PAGINATION · CURRENT PRODUCTION GEOMETRY

**`T7-L-R3 — ACCEPTED`.** **`T7-L-R4 — NOT YET ACCEPTED`.**
**`T7-L-R5 — NOT YET ACCEPTED`.**
**`T7-L-R6 — PENDING CHATGPT GOVERNANCE REVIEW`.**
**`P9 — SUPERSEDED FOR MODULE 7`** by the governed 14-logical / 18-physical
pagination rule.

**This logical source remains `ACCEPTED under T7-K` and remains ONE logical
teaching unit carrying ONE accepted logical visual.** ChatGPT has authorised its
production across **two physical frames**. **This is pagination — not new
teaching content, not two topics, not an appendix, not two optional slides.**

### Correction history — nothing erased

| Layer | Status |
|---|---|
| **Original one-frame coordinate specification** (§7–§8 above) | **Coordinate arithmetic VERIFIED and retained as history.** **Withdrawn from production** — it cannot carry the required copy at the type floors |
| **`T7-M` fabrication finding** | Minimum explicit type `7.8 pt`, 146 runs below `14 pt`. Evidence of a feasibility defect, never authority |
| **`T7-L-R3` impossibility finding** | **`ACCEPTED`** — the one-frame layout is infeasible in any arrangement; no coordinate was changed |
| **`T7-L-R4` split-production geometry** | **`NOT YET ACCEPTED`** — it split at the **authority gate**, leaving a `+0.4 pt` band on frame B and placing the page turn at the authority boundary |
| **`T7-L-R5` hardened split geometry** | **THE CURRENT PRODUCTION GEOMETRY**, defined below. Seam corrected to **`workflow | close`** on governance decision; **production fit VERIFIED**, every load-bearing region `≥ 8 pt` |

**The current production geometry is the A/B geometry below — not the infeasible
single-frame geometry of §7.**

### Logical and physical identity

| Field | Value |
|---|---|
| Logical teaching unit | **14** — title **From decision backlog to BEP workshop**, reproduced verbatim on both frames |
| Logical visual · logical source | **`M7V-14`** · **`M07-S14`** — this file |
| Physical frames | **`M07-S14-A`** (`M7V-14-A`) · **`M07-S14-B`** (`M7V-14-B`) |
| Physical deck positions | **17** and **18** of **18** |
| Continuation labels | **`PART 1 OF 2`** · **`PART 2 OF 2`** — inside the reserved title zone, **outside the title wording**, carrying **no semantic, sequential or progression meaning** |
| Parent allocation | **1.5 min** — allocated, not measured |
| Frame allocations | **1.0** + **0.5** = **1.5** ✓ — **rebalanced by `T7-L-R5`** from `0.8 + 0.7`, because the corrected seam moves Register 2 onto frame A |
| Drawn connectors | frame A **4** · frame B **0** — **`T7-L-R5`**: all four sit with the workflow core |
| New Markdown file created | **None.** Both frames are defined inside this parent source |

---

### `M07-S14-A` — physical 17 of 18 · `PART 1 OF 2` · 1.0 min · the workflow core

**Carries — the entire governed workflow core.** **Header declaration — both semantic statements, three physical lines, verbatim** · **Register 1** — `decision backlog`, `evidence gathering`, `decision agenda`, `appropriate authority engaged` · **the authority gate**, carrying `AUTHORITY REQUIRED — no automatic progression` and `evidence ≠ authority · no appropriate Triviron authority holder is currently identified or established in this programme` · **Register 2** — `candidate decision, if authorised`, `decision recorded with status`, `BEP / matrix / schedule / appendix populated only when earned`

**Geometry.** Header `x = 48–912`, `y = 84–184` — **exactly 100 pt** · Register 1 `y = 192–268`, four boxes `x = 48–249`, `269–470`, `490–691`, `711–912` — **each exactly 201 × 76 pt, three gaps of exactly 20 pt** · **authority gate `x = 48–912`, `y = 276–376` — exactly 100 pt, border ≥ 3 pt, the heaviest object on the frame** · Register 2 `y = 384–480`, three boxes `x = 48–324`, `342–618`, `636–912` — **each exactly 276 × 96 pt, two gaps of exactly 18 pt**. All three inter-band gaps exactly `8 pt`. **Seam corrected and hardened by `T7-L-R5`.**

**Geometry and text-fit arithmetic — `G1`–`G9`.**

```text
register 1:  4 × 201 + 3 × 20  =  804 + 60  =  864              ✓
register 2:  3 × 276 + 2 × 18  =  828 + 36  =  864              ✓
vertical:    100 + 8 + 76 + 8 + 100 + 8 + 96  =  396
             480 − 84                          =  396           ✓
title zone:  reserved y = 0–72 ; clear interval 84 − 72 = 12    ✓
bounds:      lowest edge 480 ≤ 520                              ✓ (G6)

REGION MARGINS  (G9, T7-L-R5):
  header declaration         need 72.8  alloc 100  +27.2  LB ✓
  Register 1, four boxes     need 51.2  alloc  76  +24.8  LB ✓
  authority gate             need 76.8  alloc 100  +23.2  LB ✓
  Register 2, three boxes    need 72.8  alloc  96  +23.2  LB ✓
  -> min LOAD-BEARING margin +23.2 ;  T7-L-R4 was +7.2
frame reserve +40.0 pt deliberately unused (rule R2).

HEADER CORRECTION, retained and enlarged.  The accepted
`3 × 18 pt = 54 pt inside 64 pt` nominal fit treated line height as
equal to point size.  Three 18 pt lines occupy 3 × 21.6 = 64.8 pt
plus 8 pt padding = 72.8 pt.  The band is now 100 pt.  Wording, the
three physical lines and the semicolon break point are unchanged.

SEAM — THIS FRAME CARRIES THE ENTIRE GOVERNED WORKFLOW CORE:
Register 1 -> authority gate -> Register 2.  The gate and the
conditional post-authority stages remain visible TOGETHER, so the
page turn happens only after the whole core has been shown and
cannot be read as progression across authority.

ALL FOUR of logical Slide 14's drawn connectors are on this frame:
three within Register 1 (1->2, 2->3, 3->4) and one within Register 2
(5->6).  NO connector 4->5.  NO connector crosses, enters, leaves or
passes behind the authority gate.  NO connector 6->7, and none enters
the final controlled-destination box — it is reached by condition,
not by arrow.                                                    ✓
```

**Connectors: `4`.**

**`PRODUCTION FIT — VERIFIED ON DECLARED BASIS` · min load-bearing region margin `+23.2 pt`.**

---

### `M07-S14-B` — physical 18 of 18 · `PART 2 OF 2` · 0.5 min · the supporting close

**Carries — the supporting close only.** **The complete nine-object method chain**, labelled `analytical sequence — nothing flows along these arrows` · **roadmap close** — the accepted Module 8 teaching handoff · **teaching / workshop boundary strip**, all three entries. **The `T7-L-R4` post-authority reminder band is removed as obsolete production mitigation.**

**Geometry.** Method chain `x = 48–912`, `y = 84–216` — **132 pt** · roadmap close `y = 224–296`, three segments **each exactly 276 pt**, two gaps of exactly 18 pt, **72 pt** tall · boundary strip `x = 48–912`, `y = 304–520` — **216 pt**. Both inter-band gaps exactly `8 pt`. **Seam corrected and hardened by `T7-L-R5`.**

**Geometry and text-fit arithmetic — `G1`–`G9`.**

```text
roadmap:   3 × 276 + 2 × 18  =  828 + 36  =  864                ✓
vertical:  132 + 8 + 72 + 8 + 216  =  436  =  520 − 84           ✓
bounds:    lowest edge 520 ≤ 520                                 ✓ (G6)

REGION MARGINS  (G9, T7-L-R5):
  method chain (nine objects)  need  98.4  alloc 132  +33.6  LB ✓
  roadmap close                need  41.6  alloc  72  +30.4  LB ✓
  teaching boundary strip      need 145.6  alloc 216  +70.4  LB ✓
  -> min LOAD-BEARING margin +30.4 ;  T7-L-R4 was +0.4

OBSOLETE MITIGATION REMOVED.  T7-L-R4's post-authority reminder band
existed only to mitigate the authority-seam page turn.  That seam is
superseded, so the band is removed.  This is removal of temporary
PRODUCTION MITIGATION, not of teaching content: it appears in no
accepted teaching source, and the full declaration plus the entire
authority gate sit complete and verbatim on frame A.

ZERO drawn connectors on this frame.  The nine-object chain's arrows
are typographic characters in a single text run.  Nothing joins the
chain, the roadmap close or the boundary strip.                   ✓
Module 8 is named as a future TEACHING MODULE — not an event and not
a scheduled workshop.                                            ✓
```

**Connectors: `0`.**

**`PRODUCTION FIT — VERIFIED ON DECLARED BASIS` · min load-bearing region margin `+30.4 pt`.**

---

### Pagination controls

| Control | Requirement |
|---|---|
| **Title** | The parent title **From decision backlog to BEP workshop** is reproduced **verbatim** on both frames — never rewritten, shortened, suffixed or qualified |
| **Continuation label** | Production navigation only. **Not teaching content, not a sequence marker, not a progression** |
| **Page transition** | **A continuation is NOT a connector.** `A → B` is never drawn as an arrow, line, bracket, chevron or leader, and no object named `page-transition` or equivalent may exist |
| **Repeated text** | Only the governing boundary is repeated, and only because rule **`G7`** requires a boundary to sit with the region it governs. Every repetition is documented in the deck specification §13.6 |
| **Copy** | **No accepted visible string was added, removed, shortened or moved to notes by `T7-L-R4`.** Every string is allocated to a frame in the assembly package's `slide-copy.md` |
| **Type floors** | `≥ 14 pt` every visible run · `≥ 18 pt` every principal label, classification, status and governance boundary. **No condensation, no reduced character spacing, no line spacing below `1.0`, no permissive shrink instruction** |
| **`G9`** | The text-fit self-check above is mandatory and was recalculated against the implemented split |

**The accepted teaching meaning, the accepted visual concept, both accepted STOP
registers and the Translation Decision Register are all unchanged.**
**`T7-M-R1` remains `NOT STARTED` and is not authorised. A produced PowerPoint
never governs this source.**

## 9. Connector semantics and prohibitions

**Four connectors exist, and only four.**

| Connector | Meaning, in words |
|---|---|
| Register 1: stage 1 → 2 → 3 → 4 (three arrows) | **`the next stage of the teaching workflow`** |
| Register 2: stage 5 → stage 6 (one arrow) | **`the next stage of the teaching workflow`** |

**Prohibited without exception:**

- **No connector crosses, enters, leaves or passes behind the gate strip**
  (`M7V-S16`). **The transition from register 1 to register 2 is conditional and
  is carried by the gate strip's words, not by a line.**
- **No connector enters the final box.** `BEP / matrix / schedule / appendix
  populated only when earned` is **reached by condition, not by arrow**
  (`M7V-S18`, rule **K7**).
- **No connector touches the method one-liner, the roadmap close or the boundary
  strip.**

**The method one-liner's own arrows are typographic characters inside a single
text run, not drawn connectors**, and the run is labelled `analytical sequence —
nothing flows along these arrows`.

## 10. Typography, status and typed-absence treatment

**Minimum visible type 14 pt · principal labels ≥ 18 pt · every header, gate,
one-liner and boundary word ≥ 18 pt · contrast ≥ 4.5 : 1 · border and connector
contrast ≥ 3 : 1.**

**Header declaration typography — mandatory:** single (1.0) line spacing · no
paragraph spacing before or after · **no font condensation** · **no reduced
character spacing used merely to force fit** · **internal top and bottom margins
must allow the three 18 pt declaration lines to sit cleanly inside the current
`100 pt` header band** (`T7-L-R6`; the `64 pt` figure it replaced belongs to the
superseded one-frame geometry of §7). **Three 18 pt lines occupy `3 × 21.6 =
64.8 pt` of line height plus `8 pt` padding = `72.8 pt`, leaving `+27.2 pt`.**
**AutoFit is off; if the lines do not fit, STOP and refer back.**

**Mandatory warning:** the header declaration's two lines and the gate strip's
two lines, **each pair together**, plus the boundary strip in full.

**Typed absence.** **Register 2 is not styled as unreached, blocked, greyed or
pending** — it is a later part of a teaching workflow. **The `only when earned`
condition is a stated condition inside its own box**, never a lock, a barrier, a
padlock or a greyed state. **The handed-forward zeros are stated neutrally and
are not drawn as work the workshop will erase.** **`NONE IDENTIFIED` is not
`NONE EXISTS`.**

**Status meaning is carried by words, never by colour alone.**

## 11. Source, authority and teaching-synthesis labels

**`TEACHING SYNTHESIS`** — the third boundary-strip entry (*a good BEP workshop
does not start with answers. It starts with the decisions that must be earned.*)
is **not a quoted source sentence**. It carries the words `TEACHING SYNTHESIS` on
the slide, is visually distinguishable from quoted framework, register and schema
wording, and the distinction is carried **by that word or a separate labelled
zone — never by colour or italics alone**.

**The whole workflow is declared as teaching**, in the header, before any stage
is read. **The roadmap close names a module, not an event.**

## 12. Build sequence

> **`T7-L-R5` — this sequence is the SUPERSEDED one-frame build.** It is retained
> as history. **The current build is the two-frame sequence in the `T7-L-R4` /
> `T7-L-R5` pagination section below**: frame A = header (`100 pt`) · Register 1 ·
> authority gate · Register 2 · **all four connectors**; frame B = method chain ·
> roadmap close · boundary strip · **no connectors**. **The `64 pt` header band
> below is arithmetically wrong and is not to be built** — see the header
> correction in that section.

1. **Header declaration — build the 64 pt band first, then place both semantic
   statements in it at ≥ 18 pt across their three authorised physical lines, and
   confirm the fit before proceeding. If they do not fit, STOP.**
2. Register 1 boxes.
3. Three register-1 arrows.
4. **Gate strip, with both lines already present.**
5. Register 2 boxes 5 and 6.
6. The single 5 → 6 arrow.
7. **Box 7, with its `only when earned` condition already present and no arrow
   into it.**
8. Method one-liner in full.
9. Roadmap close.
10. Boundary strip.

## 13. Mandatory omissions

**No meeting room · no calendar · no date · no participant · no seat · no table ·
no workshop photograph · no agenda · no scheduled event · no clock · no
duration.**

Also: no connector across the gate strip · no connector into box 7 · no tick · no
percentage · no progress bar · no authority holder · no owner · no organisation ·
no platform · no Triviron BEP clause or section number · **no register value
changed**.

## 14. Accessibility and reading order

**Reading order:** header declaration → register 1 stages in order → **gate strip
in full** → register 2 stages in order → method one-liner in full → roadmap
close, three segments → boundary strip in full.

**The header declaration is read as its two semantic statements, not as its three
physical lines** — the line break at the semicolon is layout only and is **not
announced**; the second statement is read as one continuous sentence.

**The absence of any connector across the gate strip and into the final stage
must be stated in words.** The one-liner's arrows are read as *then*, and the
sequence described as analytical. Governance wording is readable, not decorative.

## 15. Screen-reader description

*"A band headed 'teaching workflow — not a current Triviron workflow; no actual
Triviron workshop or schedule is established in this programme, and this diagram
does not assert or schedule one'. First register, four stages joined by arrows
meaning only the next stage of the teaching workflow: decision backlog; evidence
gathering; decision agenda; appropriate authority engaged. Between the registers,
a full-width gate strip: authority required — no automatic progression; evidence
is not authority; no simulated Triviron decision function has been authorised,
and no appropriate Triviron authority holder is currently identified or
established in this programme. No arrow crosses this strip, and none exists.
Second register: candidate decision, if authorised; an arrow to decision recorded
with status; and then, reached by condition and not by any arrow, BEP, matrix,
schedule or appendix populated only when earned. Beneath, the complete method in
one line, an analytical sequence along which nothing flows: Harrismith reference
pattern, then translation question, then Triviron evidence required, then
evidence available or missing, then STOP, then candidate decision, then authority
required, then recorded status, then controlled destination. Beneath that, a
roadmap close in three segments: completed teaching foundation, Modules 1 to 6;
Module 7, translation method and decision backlog; next, Module 8, BEP workshop
facilitation — a teaching module, not an event. At the foot: the future workshop
is not 'copy Harrismith into Triviron' and not 'fill in the blanks' — it is a
controlled decision-making process in which the questions, required evidence,
authority and status are made explicit; the backlog handed forward is unchanged,
twenty-one rows, evidence none identified twenty-one times, candidate decisions
zero; and, as teaching synthesis, a good BEP workshop does not start with
answers, it starts with the decisions that must be earned."*

## 16. Native reconstruction

> **`T7-L-R6` — SUPERSEDED ONE-FRAME METHOD, RETAINED AS HISTORY. Do not build
> from this sequence.** **The current production geometry is the two-frame `T7-L-R5` A/B geometry** —
see the **`T7-L-R4` / `T7-L-R5` GOVERNED SPLIT PAGINATION** section below.
Frame A: header `y = 84–184` (**100 pt**) · Register 1 `y = 192–268` · authority
gate `y = 276–376` · Register 2 `y = 384–480` · **all four connectors**.
Frame B: method chain `y = 84–216` · roadmap close `y = 224–296` · boundary strip
`y = 304–520` · **zero connectors**.
> **The current build sequence and its per-region text-fit arithmetic are in the
> pagination section above, and the build instrument is
> [`../../module-07-harrismith-to-triviron-bep-translation/presentation/production-checklist.md`](../../module-07-harrismith-to-triviron-bep-translation/presentation/production-checklist.md).**

1. **Draw the header rectangle at `x = 48–912`, `y = 84–148` — exactly 64 pt.**
   Place **one text box** holding both semantic statements at ≥ 18 pt, single
   line spacing, no paragraph spacing, **no condensation, no reduced character
   spacing**. The second statement wraps at its semicolon onto a third physical
   line. **Confirm the three lines fit; if not, STOP and refer back.**
2. Draw four register-1 rectangles; confirm exactly 201 × 68 pt with 20 pt gaps.
3. Draw three arrows within register 1 only.
4. Draw the heavy-bordered gate strip at `y = 232–272`; place both lines.
   **Confirm no connector touches it.**
5. Draw three register-2 rectangles; confirm exactly 276 × 68 pt with 18 pt gaps.
6. Draw one arrow, stage 5 → stage 6. **Draw no arrow into box 7.**
7. Place the method one-liner as a single full-width text box, arrows as
   typographic characters.
8. Draw three roadmap-close rectangles at exactly 276 pt with 18 pt gaps.
9. Draw the bordered boundary strip; place all three runs, with
   `TEACHING SYNTHESIS` labelled in words.

**Not used and not required:** PNG · JPG · SVG · screenshot · logo · external
icon · rasterised text · Mermaid · SmartArt · external renderer · network asset.

**`Mermaid PROHIBITED`** — a flowchart engine would connect stage 4 to stage 5
and stage 6 to stage 7 as a matter of course, producing exactly the automatic
progressions rules **K6** and **K7** forbid.

## 17. Producer-failure test

**Accepted semantic test, carried unweakened from `M7V-14`:**

> **Ask a reader when the workshop happens.** If any answer — a date, a stage,
> *after Module 8*, *soon* — is offered, **STOP**. **Then ask who the appropriate
> authority is.** If any answer is offered, the gate strip has failed. **Then
> trace the workflow with a finger from stage 1 to stage 7.** If the finger
> crosses the gate strip without stopping, or reaches stage 7 along an arrow,
> **STOP**.

**Source-reconstruction test (added by `T7-K`; semantic test unaltered;
dimensions updated to the current geometry by `T7-L-R6`):** count connectors
across the logical unit — the count must be exactly **four**, three in Register 1
and one in Register 2, **and after the `workflow | close` seam all four sit on
frame A, with frame B carrying zero**. Then verify each frame's vertical
arithmetic: **frame A `100 + 8 + 76 + 8 + 100 + 8 + 96 = 396`, lowest edge
`480`**; **frame B `132 + 8 + 72 + 8 + 216 = 436`, lowest edge exactly `520`**.
Then verify the three header lines sit at ≥ 18 pt inside the **current `100 pt`**
header band **without condensation and with AutoFit off**. If any check fails,
**STOP and refer back**.

## 18. STOP conditions

**Accepted teaching STOPs (`M7-Snn`) — unchanged, cited not restated:**
`M7-S10` · `M7-S11` · `M7-S15` · `M7-S17` · `M7-S18`.

**Accepted visual-production STOPs (`M7V-Snn`) — unchanged:**
`M7V-S03` · `M7V-S04` · `M7V-S08` · `M7V-S09` · `M7V-S10` · `M7V-S11` ·
`M7V-S13` · `M7V-S14` · `M7V-S16` · `M7V-S17` · `M7V-S18` · `M7V-S20` ·
`M7V-S26` · `M7V-S28` · `M7V-S30` · `M7V-S31` · `M7V-S32` · `M7V-S33` ·
`M7V-S34`.

**The two registers are separate spaces.**

## 19. Module 8 grammar reuse

**`REUSABLE INTO MODULE 8` — `EMPTY GRAMMAR ONLY`, and with the greatest care of
any visual in the module.**

Reusable: the **declared teaching-workflow band**; the **authority gate strip
that no connector crosses**; the **condition-reached final object with no
incoming arrow**; the **controlled-destination classes** treatment; and the
**evidence ≠ authority** statement.

**Not reusable:** the seven stage labels · the method one-liner · the roadmap
close wording · the handed-forward counts · any Module 7 register value.

**Module 8 teaches facilitation method; it does not inherit Module 7's workflow
content, and it creates and approves no Triviron governance.**

## 20. Rendered-asset status

**`NONE`.** No rendering attempted, no renderer installed, no image, SVG, PDF or
Office file produced. See [`rendered/README.md`](rendered/README.md).
